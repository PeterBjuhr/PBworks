\paper {
   % spacing between systems
  system-system-spacing = 
  #'((basic-distance . 21)
       (minimum-distance . 19)
       (padding . 16)
       (stretchability . 11))
  
  last-bottom-spacing = #'((basic-distance . 21)
                           (minimum-distance . 19)
                           (padding . 16)
                           (stretchability . 11))
  
  max-systems-per-page = #'2
}