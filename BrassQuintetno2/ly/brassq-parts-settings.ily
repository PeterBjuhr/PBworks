\paper {
  system-system-spacing =
  #'((basic-distance . 11)
     (minimum-distance . 6)
     (padding . 5)
     (stretchability . 11))

  last-bottom-spacing = #'((basic-distance . 11)
                           (minimum-distance . 6)
                           (padding . 5)
                           (stretchability . 11))

  %max-systems-per-page = #'2
}